#include<bits/stdc++.h>
using namespace std;
int main(){
    string s1, s2;
    cin >> s1 >> s2;
    cout << s1 << " " << s2; //outputs 2 strings
    string s;
    getline(cin, s);    //outputs unit enter(linebreak), unlike strings which read until whitespaces
    cout << s;
    string egz = " double quotes initialization for strings";   
    cout << egz;
    return 0;
}